let preco = 100;
let desconto = 0.2;
let precoComDesconto = (preco * desconto);
let precoFinal = preco - resultado;
 alert(`O preço do produto é R$ ${preco}, com desconto de 20% sai por R$ ${precoFinal}.`);